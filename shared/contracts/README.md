# Shared API contracts

Schemas describe current Hub/agent payloads. They are documentation and validation targets; they do not silently change production wire format.

`draft/telemetry-receiver-v2.schema.json` is reserved for future receiver-only phone correctness metadata and is **not active** in r1.
